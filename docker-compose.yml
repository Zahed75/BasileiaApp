
version: '3'

networks:
  basileia-production-network:
    driver: bridge

services:
  basileia_production_backend:
    container_name: basileia_production_backend
    restart: always
    build:
      context: .
      dockerfile: Dockerfile
    networks:
      - basileia-production-network
    env_file:
      - .env
    volumes:
      - .:/app
    ports:
      - "127.0.0.1:17001:8080"
    depends_on:
      - basileia_production_mongo

  basileia_production_mongo:
    container_name: basileia_production_mongo
    restart: always
    image: mongo
    ports:
      - "17002:27017"
    networks:
      - basileia-production-network
    volumes:
      - /home/basileia_persistent/mongodb:/data/db
    env_file:
      - .env

  basileia_production_mongo_express:
    container_name: basileia_production_mongo_express
    image: mongo-express
    restart: always
    ports:
      - "127.0.0.1:17003:8081"
    networks:
      - basileia-production-network
    env_file:
      - .env
    depends_on:
      - basileia_production_mongo